import { styled } from './theme.ts'

export const HeaderContainer = styled('header', {
  textAlign: 'center',

  p: {
    fontWeight: 'bold',
    marginBottom: '24px',

  },
  h1: {
    marginBlock: '.5rem',
    marginBottom: '1rem',
    fontSize: '1.8rem',
  },
})
