import { styled } from './theme.ts'

export const Container = styled('div', {
  width: '100%',

  h1: {
    width: '100%',
    textAlign: 'center',
    marginBottom: '1rem',
    fontSize: '1.2rem',
  },
})
